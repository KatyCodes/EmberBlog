<div class="well">
  {{#link-to 'post' post.id}}
  <h3>{{post.title}}</h3>
  <h4><em>By: </em><strong>{{post.author}}</strong></h4>
  <img src="{{post.image}}">
  <h5>{{post.date}}</h5>
  {{/link-to}}
</div>
